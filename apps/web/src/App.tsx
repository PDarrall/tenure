import { tunables } from '@tenure/engine'

/** Placeholder until the game screens land in the next commit. */
export function App() {
  return (
    <main>
      <h1>Tenure</h1>
      <p>Browser play is being wired up. Default seed {tunables.DEFAULT_SEED}.</p>
    </main>
  )
}
